# BUGS

> This file is for workflow/tooling failures and implementation bugs only.
> Reusable copy or visual feedback belongs in `accounts/antigpt/README.md`
> (format rules) or `accounts/antigpt/account-brief.md` (voice and audience),
> not here.

Entry format:

```text
## YYYY-MM-DD — short title

- What broke:
- Root cause:
- Fix:
- Pattern to avoid:
```

No open entries.
