# Product

Shared w(inner) product source of truth.

Read these files before any product mention:

1. `app-brief.md` for product facts, positioning, user segments, and language.
2. `claim-bank.md` for approved, flagged, and rejected claims.

Assets belong in `assets/`. When an asset becomes approved for social use, add it
to the asset table in `app-brief.md`.
